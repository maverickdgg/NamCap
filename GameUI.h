#ifndef _GAMEUI_H
#define _GAMEUI_H

#include<iostream>
#include<fstream>
#include<string>
#include"map.h"

using std::cout;
using std::endl;
using std::string;
using std::getline;
using std::ifstream;

void insertmap(PMAP sMap);
void printText(PMAP text);
//void insertmap1(short** sMap);
#endif